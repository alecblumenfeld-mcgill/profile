{
  "cmd": ["perl", "${file}"],
  "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
  "working_dir": "${file_path}",
  "selector": "source.perl",

  "variants":
  [
    {
      "name": "Run",
       "cmd": ["perl", "-c" ,"${file}"]
    }
  ]
}
